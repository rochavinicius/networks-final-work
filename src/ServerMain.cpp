#include <iostream>

#include "../headers/services/ServerService.h"

// Compile: g++ src/models/Client.cpp src/ServerMain.cpp -o server
int main(int argc, const char *argv[])
{
    /*
    initialize a verification unit and a flow control unit to pass to the client and server
    */

    // ServerService serverService; //(/* pass all the parameters */);

    // serverService.Start();

    return 0;
}