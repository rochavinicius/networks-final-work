#include <iostream>

#include "../headers/services/ClientService.h"

int main(int argc, const char *argv[])
{
    /*
    initialize a verification unit and a flow control unit to pass to the client and server
    */

    // ClientService clientService; //(/* pass all the parameters */);

    // clientService.Start();

    return 0;
}